# =================================
# ARCHIVO DE EJEMPLO - NO USAR DIRECTAMENTE
# Django Secret Key - Genera uno nuevo en: https://djecrety.ir/
# ⚠️ IMPORTANTE: Este SECRET_KEY debe ser EL MISMO en auth-service y SalumedX
#    Para que la validación JWT funcione correctamente entre microservicios
SECRET_KEY=your-secret-key-change-in-production-xyz123

# Debug mode - SIEMPRE False en producción
DEBUG=False

# Hosts permitidos - Dominio de Render + localhost para desarrollo
# Formato: tu-app.onrender.com,localhost,127.0.0.1
ALLOWED_HOSTS=.onrender.com,localhost,127.0.0.1

# Auth Service URL (para comunicación entre microservicios)
AUTH_SERVICE_URL=http://localhost:8001

# Permitir todos los orígenes CORS (True) o específicos (False)
CORS_ALLOW_ALL_ORIGINS=True

# Si CORS_ALLOW_ALL_ORIGINS=False, especifica los orígenes permitidos
# Agrega el dominio de tu frontend separado por comas
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Zona horaria
TZ=America/Guayaquil

# NOTA: DATABASE_URL se configura automáticamente en Render cuando creas una base de datos PostgreSQL
# =================================

SECRET_KEY=tu-secret-key-aqui
DEBUG=False
ALLOWED_HOSTS=.onrender.com,localhost,127.0.0.1

# Base de datos (Render la configura automáticamente)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# CORS - Agrega tus dominios frontend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
CORS_ALLOW_ALL_ORIGINS=True

# Archivos estáticos
STATIC_URL=/static/
STATIC_ROOT=staticfiles

# Zona horaria
TZ=America/Guayaquil

# Puerto
PORT=8000
